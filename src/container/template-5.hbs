<header class="header row g-0">
  <div class="logo">
    {{>logo}}
  </div>
  {{!-- посилання справа сторінки --}}
  <nav class="header_nav col-9 ">
    <div class="header_nav_right">
      {{!-- ітeрація списка зі змінною header --}}
      {{>nav list=header}}
    </div>
  </nav>
</header>


{{!--block для списка та форми--}}
<main class="main">
  {{#with messages}}
  <div class="messages">
    <h1 class="messages_title">{{title}}</h1>
    {{#each list}}
    <div class="msg">
      <strong class="=msg_name">{{name}}</strong>
      <p class="msg_text">{{message}}</p>
      <span class="msg_day">{{day}}</span>
    </div>
    {{/each}}


    <a href="#" class="messages_link">All messages</a>
  </div>

  {{/with}}
  {{!-- форма --}}

  {{#with form}}
  <form class="form">
    <h2 class="form_title">{{title}}</h2>
    {{!-- ітеруем инпути --}}
    <div class="form_input_list">
      {{#each inputs}}
      <input type="{{type}}" placeholder="{{placeholder}}" class="form_input" name="{{name}}" id="form-input-{{id}}">
      {{/each}}
    </div>

    {{!-- чекбокс --}}

    <div class="form_checkbox">
      <input type="checkbox" name="{{checkbox.name}}" id="{{checkbox.name}}">
      {{!-- имитация инпута и стилизация чепез пустій див --}}
      <div></div>
      <label for="{{checkbox.name}}">{{checkbox.label}}</label>
    </div>


    {{!-- кнопка --}}
    <button type="submit" class="form_button">{{button}}</button>
    {{!-- текст під кнопкою --}}
    <p class="form_info">{{info}}</p>

  </form>
  {{/with}}

  <div class="advantages">
    {{#each advantages}}
    <div>
      <strong>{{title}}</strong>
      <p>{{text}}</p>
    </div>
    {{/each}}
  </div>

</main>


{{!-- block 3 card --}}
{{!-- <div class="advantages">
  {{#each advantages}}
  <div>
    <strong>{{title}}</strong>
    <p>{{text}}</p>
  </div>
  {{/each}}
</div> --}}







{{!-- блок футера --}}

<footer class="row footer">
  <div class="col-3">
    {{>logo}}
    <span class="footer_copyright">Copyright 2023-2024</span>
  </div>
  {{!-- ітерація змінної title--}}
  {{#each footer}}
  <div class="col-3">
    <span class="footer_title">{{title}}</span>
    {{!-- ітeрація списка зі змінною links вертикально --}}
    {{>nav list=links isVertical=true}}
  </div>
  {{/each}}
</footer>




</div>